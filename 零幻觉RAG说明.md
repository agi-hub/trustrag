# 无Embedding、零幻觉Agentic RAG（Markdown仓库多轮检索）需求说明书

## 1\. 整体定位与核心约束（刚性红线，不可突破）

### 1\.1 系统定位

构建**纯大模型驱动、无Embedding向量检索、无文本生成、无内容改写**的Agentic RAG系统，检索数据源为本地/远程Markdown文档仓库。系统仅通过大模型完成多轮搜索规划、检索词迭代、结果筛选排序，最终输出内容**100%源自Markdown原文摘抄**，彻底杜绝大模型幻觉。

### 1\.2 零幻觉刚性规则（强制落地）

- **禁止一切内容生成**：不总结、不转述、不融合、不推理、不补全、不续写任何文档内容

- **禁止篡改原文**：所有对外展示的文本均为Markdown原文逐字摘抄，无删减改写、无语义润色

- **禁止基座知识兜底**：大模型仅执行搜索调度、筛选、排序逻辑，禁止使用模型自有知识库作答

- **禁止主观结论输出**：不判定内容对错、不对比信息优劣、不输出主观解读，仅客观陈列检索结果

### 1\.3 核心技术方案

舍弃Embedding向量检索、向量切片、向量数据库全链路，仅采用：**大模型Agent多轮搜索规划 \+ Markdown仓库全文/关键词检索 \+ 原文摘抄输出**架构。

## 2\. 核心能力定义

### 2\.1 Agent唯一合法能力

大模型Agent仅可执行「搜索调度类操作」，无任何内容创作权限：

1. 解析用户原始查询意图，拆解初始检索关键词/短句

2. 基于已检索到的Markdown内容，判断信息缺口，规划**新一轮迭代搜索词**（多轮Agent搜索）

3. 校验检索结果相关性，过滤无效、噪声、无关文档片段

4. 对有效检索结果做相关性概率打分、优先级排序

5. 记录完整多轮搜索路径、搜索关键词、检索执行过程

6. 识别Markdown原文内置的章节页码标记，结构化提取元信息

### 2\.2 Agent绝对禁止能力

- 禁止扩写用户原始意图、新增无关检索维度、发散搜索范围

- 禁止改写、拼接、概括、浓缩Markdown原文内容

- 禁止基于多段原文推导结论、整合信息、生成答案

- 禁止主观判定文档内容正确性，仅做相关性筛选

## 3\. 数据源规范（Markdown仓库）

### 3\.1 文档格式要求

所有检索文件为标准Markdown格式，文件内**固定内置标准化章节页码标记**，为系统必填识别字段，标记格式如下：

`<!-- page: 435 | book: Programming Massively Parallel Processors (Third Edition) | chapter: 19 Parallel programming with OpenACC > 19.3 OpenACC by Example > OpenACC Loop Optimizations -->`

### 3\.2 标记字段拆解（系统需自动解析）

- **page**：原文页码

- **book**：所属书籍名称

- **chapter**：一级章节名称

- **次级目录**：标记内层级章节（19\.3 OpenACC by Example、OpenACC Loop Optimizations）

## 4\. Agent多轮搜索执行流程（标准闭环）

全程无人工干预，程序自动迭代，配置最大搜索轮次上限（默认3轮，可配置，防止无限循环）

### 4\.1 第一轮：初始搜索规划

1. Agent解析用户Query，拆解精准检索关键词/短句（无发散扩写）

2. 基于关键词遍历检索Markdown仓库所有文件

3. 初步筛选有效片段，过滤无匹配内容

### 4\.2 迭代多轮搜索（Agent核心逻辑）

1. Agent读取当前已获取的所有Markdown原文片段

2. 客观判定当前信息是否**完整覆盖用户查询维度**（无主观脑补）

3. 存在信息缺口：生成新一轮精准检索词（严格贴合用户原始问题，无新增实体），发起下一轮检索

4. 无新增有效信息/信息全覆盖：终止多轮搜索

5. 连续两轮无新增有效内容：强制终止检索（防无效迭代）

### 4\.3 后置统一处理（无内容修改）

1. 汇总所有轮次检索结果，全局去重、剔除重复片段

2. Agent对所有有效片段做**相关性概率打分（0\-100%）**

3. 按相关性概率降序排序

4. 自动解析每段原文对应的Markdown章节页码标记

## 5\. 最终输出报告规范（程序自动生成，固定格式）

输出内容分为两大模块：**多轮搜索路径日志** \+ **结构化原文检索结果**，全程无人工润色、无模型总结创作，仅程序格式化打印。

### 5\.1 模块一：完整多轮搜索路径日志

逐条记录每一轮检索的核心信息，保证全链路可审计、可溯源：

- 本轮搜索轮次（第N轮）

- 本轮使用的所有检索关键词/短句

- 本轮检索命中的Markdown文件路径

- 本轮检索结果判定（获取新增有效内容/无新增内容）

- 下一轮搜索规划依据（若无则标注「检索终止」）

### 5\.2 模块二：结构化检索结果（核心输出）

单条结果固定包含6个字段，**所有字段除概率外均为原文原生内容**：

1. **文件路径**：命中的Markdown文件完整仓库路径

2. **章节页码元数据**：自动解析原文注释标记（page/book/章节层级）

3. **相关性概率**：Agent客观打分（0\-100%，仅用于排序参考）

4. **原文摘抄**：逐字复制匹配的Markdown原文片段，无任何修改

5. **命中检索词**：本条内容对应的本轮搜索关键词

6. **检索轮次**：本条内容命中的搜索轮次

## 6\. 标准输出示例（最终成品样式）

===================== Agentic RAG 多轮检索日志 =====================

第1轮搜索

检索关键词：CXL 3\.1 内存池化特性、CXL3\.0与3\.1版本差异

检索文件路径：/docs/parallel\-compute/openacc\-feature\.md

检索判定：命中基础特性内容，缺失版本对比维度，启动第二轮检索



第2轮搜索

检索关键词：CXL3\.0 vs CXL3\.1 内存池化能力对比

检索文件路径：/docs/parallel\-compute/cxl\-version\-compare\.md

检索判定：信息维度全覆盖，终止多轮检索



===================== 排序后检索结果（原文摘抄） =====================

【1】文件路径：/docs/parallel\-compute/cxl\-version\-compare\.md

章节信息：page: 435 \| book: Programming Massively Parallel Processors \(Third Edition\) \| chapter: 19 Parallel programming with OpenACC \> 19\.3 OpenACC by Example \> OpenACC Loop Optimizations

相关性概率：98%

命中检索词：CXL3\.0 vs CXL3\.1 内存池化能力对比

检索轮次：第2轮

原文摘抄：【此处为Markdown原文完整片段，逐字无修改输出】



【2】文件路径：/docs/parallel\-compute/openacc\-feature\.md

章节信息：page: 428 \| book: Programming Massively Parallel Processors \(Third Edition\) \| chapter: 19 Parallel programming with OpenACC \> 19\.2 OpenACC Basic Syntax

相关性概率：92%

命中检索词：CXL 3\.1 内存池化特性

检索轮次：第1轮

原文摘抄：【此处为Markdown原文完整片段，逐字无修改输出】

## 7\. 异常场景处理规范

- **无任何检索结果**：程序固定输出「多轮检索完成，未在Markdown仓库中查询到相关原文内容」，无额外脑补内容

- **检索无新增内容**：自动终止迭代，提示「后续检索无新增有效信息，提前终止搜索」

- **原文无章节标记**：标注「未识别到标准化章节页码信息」，不编造、不补全元数据

- **多片段信息冲突**：仅保留各自原文\+独立概率打分，不评判对错，仅客观陈列

## 8\. 核心落地优势

1. **绝对零幻觉**：无任何模型生成内容，所有有效信息均为仓库原文摘抄

2. **架构极简**：无需Embedding、无需向量库、无需切片预处理，仅依赖Markdown文件检索\+Agent调度

3. **全链路透明**：完整记录每一轮搜索关键词、路径、过程，可审计、可追溯

4. **智能不越界**：保留Agent多轮迭代搜索的智能性，同时严格锁死内容输出底线

> （注：部分内容可能由 AI 生成）
